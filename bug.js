This bug occurs when using the Expo Camera API with a custom component. The camera preview renders correctly, but taking a picture results in a blank image. This happens inconsistently, sometimes working and other times failing without any clear pattern or error message in the console.